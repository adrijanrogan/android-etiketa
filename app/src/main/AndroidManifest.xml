<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.adrijanrogan.etiketa">

    <!-- Za branje in pisanje rabimo pravico -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- StartActivity se pokaže uporabniku ob zagonu aplikacije -->
        <!-- noHistory poskrbi, da se uporabnik ne more vrniti v StartActivity, ko jo zapusti -->
        <activity android:name="com.github.adrijanrogan.etiketa.ui.StartActivity"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- V AndroidManifest moramo deklarirati vse svoje komponente -->
        <activity android:name="com.github.adrijanrogan.etiketa.ui.BrowserActivity" />
        <activity android:name="com.github.adrijanrogan.etiketa.ui.MetadataActivity" />

    </application>

</manifest>